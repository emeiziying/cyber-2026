# [项目名称] — AI 助手规则

## 项目背景
[简短描述：这是什么项目，主要功能，目标用户]

---

## 技术栈
- **语言：** TypeScript 5.x（严格模式）
- **运行时：** Node.js 20+
- **框架：** [Express / Fastify / NestJS]
- **数据库：** [PostgreSQL via Prisma / MongoDB / ...]
- **测试：** [Vitest / Jest]
- **CI/CD：** [GitHub Actions / ...]

---

## 常用命令
```bash
# 开发
npm run dev

# 测试
npm test
npm run test:coverage

# 构建
npm run build

# 数据库迁移
npx prisma migrate dev
```

---

## 目录结构
```
src/
├── routes/         # 路由定义（仅路由，不含业务逻辑）
├── services/       # 业务逻辑层
├── repositories/   # 数据访问层
├── middleware/     # Express 中间件
├── utils/          # 工具函数
└── types/          # TypeScript 类型定义
```

---

## 代码规范

### 命名约定
- 文件名：`kebab-case`（如 `user-service.ts`）
- 类名：`PascalCase`
- 函数/变量：`camelCase`
- 常量：`UPPER_SNAKE_CASE`
- 数据库字段：`snake_case`

### 接口返回格式
统一使用以下格式：
```typescript
// 成功
{ data: T, error: null }
// 失败
{ data: null, error: { code: string, message: string } }
```

### Import 顺序
1. Node.js 内置模块
2. 第三方库
3. 内部模块（绝对路径 `@/`）
4. 相对路径模块

---

## 禁止行为
- 不要使用 `any` 类型（如必须使用，添加注释说明原因）
- 不要在 routes 层写业务逻辑，业务逻辑放在 services 层
- 不要直接在代码中硬编码配置值，使用 `src/config` 模块
- 不要在 API 错误响应中暴露内部信息（堆栈、数据库错误等）
- 不要使用 `console.log`，使用项目的 `logger` 工具
- 不要跳过类型检查，不要使用 `@ts-ignore`（除非有充分理由并注释）

---

## 安全规范
- 所有用户输入必须通过 `zod` 验证
- 敏感操作（删除、权限变更）必须有操作日志
- SQL 查询必须通过 ORM，不允许拼接原始 SQL

---

## 测试规范
- 每个 service 方法都要有对应单元测试
- 测试文件放在与被测文件同目录，命名为 `*.test.ts`
- Mock 外部依赖（数据库、HTTP 请求）

---

## 当前开发重点
[可选：填写当前迭代的重点功能或需要特别注意的模块]
