# Harness Engineering 建设检查清单

**团队：** _______________
**日期：** _______________
**评估人：** _______________

---

## 第一层：Context Engineering（上下文工程）

### CLAUDE.md 完整性

- [ ] 项目架构约定已记录（分层规范、禁止行为、模块边界）
- [ ] 编码规范已记录（命名、格式、类型使用约定）
- [ ] 常用工具与路径已记录（文档地址、示例文件路径、关键命令）
- [ ] 最近一次更新时间：_______________（超过 30 天请及时复审）

### 动态上下文（MCP / 工具访问）

- [ ] Agent 可以访问项目文档（只读）
- [ ] Agent 可以查询数据库 schema 或 API spec（如适用）
- [ ] Agent 无需人工粘贴上下文即可完成常见任务

### Context 质量

- [ ] 新成员不需要口头解释，仅靠 CLAUDE.md 就能让 Agent 生成符合团队规范的代码
- [ ] 已有机制确保 Context 随项目演进同步更新（如：每次架构决策后更新 CLAUDE.md）

**Context Layer 评分（1–5）：** ___
**主要缺口：** _______________

---

## 第二层：Architectural Constraints（架构约束）

### 自动化检查

- [ ] 关键架构约束有自动检查（如：禁止绕过 service 层、禁止直接访问数据库）
- [ ] 代码格式/lint 检查在 CI 中运行
- [ ] Agent 触发的自动检查在错误时输出清晰的修复提示

### 错误日志

- [ ] 团队维护 Agent 错误日志（记录 Agent 产生过的需要人工修正的问题）
- [ ] 错误日志定期（至少每两周）过一遍，高频错误已转化为新约束
- [ ] 错误日志条目数：_______________ 条（已修复：_______________）

### Constraints 覆盖率

- [ ] 最近 10 个 PR 中，被 Review 指出的 AI 产出问题中，有自动检查覆盖的比例 ≥ 70%

**Constraints Layer 评分（1–5）：** ___
**主要缺口：** _______________

---

## 第三层：Garbage Collection（垃圾回收）

- [ ] 有定期运行的 GC 任务（建议：每 1–2 周）
- [ ] GC 检查范围明确（如：文档一致性、测试覆盖、依赖过期）
- [ ] GC 输出为 Issue 或 PR draft，经人工审核后处理（非直接修改代码）
- [ ] 上次 GC 运行时间：_______________

**GC Layer 评分（1–5）：** ___
**主要缺口：** _______________

---

## 综合评估

| 层 | 评分（1–5） | 优先改进项 |
|----|-------------|------------|
| Context Engineering | | |
| Architectural Constraints | | |
| Garbage Collection | | |
| **整体** | | |

### 本次行动计划

| 行动 | 负责人 | 完成时间 |
|------|--------|----------|
| | | |
| | | |
| | | |

---

## Harness 健康指标（可选追踪）

- 每 PR 被 Review 指出的 AI 产出问题均值：_______________ 个
- Agent 生成代码需要人工修改的比例：_______________ %
- CLAUDE.md 上次更新距今天数：_______________ 天
- 错误日志中已固化为规则的比例：_______________ %

---

*本模板基于 Cyber 2026 — Harness Engineering 章节。建议每季度或每次重大架构变更后重新评估。*
