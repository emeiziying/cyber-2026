# [项目名称] — AI 助手规则

## 项目背景
[简短描述：这是什么项目，主要功能，目标用户]

---

## 技术栈
- **语言：** TypeScript 5.x（严格模式）
- **运行时：** Node.js 20+
- **框架：** [Express / Fastify / NestJS]
- **数据库：** [PostgreSQL via Prisma / MongoDB / ...]
- **测试：** [Vitest / Jest]
- **CI/CD：** [GitHub Actions / ...]

---

## 常用命令
```bash
# 开发
npm run dev

# 测试
npm test
npm run test:coverage

# 构建
npm run build

# 数据库迁移
npx prisma migrate dev
```

---

## 目录结构
```
src/
├── routes/         # 路由定义（仅路由，不含业务逻辑）
├── services/       # 业务逻辑层
├── repositories/   # 数据访问层
├── middleware/     # Express 中间件
├── utils/          # 工具函数
└── types/          # TypeScript 类型定义
```

---

## 代码规范

### 命名约定
- 文件名：`kebab-case`（如 `user-service.ts`）
- 类名：`PascalCase`
- 函数/变量：`camelCase`
- 常量：`UPPER_SNAKE_CASE`
- 数据库字段：`snake_case`

### 接口返回格式
统一使用以下格式：
```typescript
// 成功
{ data: T, error: null }
// 失败
{ data: null, error: { code: string, message: string } }
```

### Import 顺序
1. Node.js 内置模块
2. 第三方库
3. 内部模块（绝对路径 `@/`）
4. 相对路径模块

---

## 行为规范（用触发器格式描述，比形容词更精确）

> 格式：在 [具体场景] 时，[具体行为]

### 代码质量
- 函数体超过 50 行时，拆分为职责单一的子函数
- 看到超过 3 层嵌套的回调或 if 时，重构为 async/await 或提前 return
- 发现重复逻辑出现 3 次以上时，提取为工具函数并放入 `src/utils/`
- 写新功能前，先用 Grep 搜索是否有可复用的现有实现

### 错误处理
- 遇到 try-catch 只写 `console.log` 的情况，要求补充错误上报（用 `logger.error`）
- API 接口发生错误时，返回通用错误信息给前端，详细错误写入日志，绝不暴露堆栈或数据库报错
- 调用外部服务时，确保有超时设置和失败重试逻辑

### 安全
- 处理任何来自用户、HTTP 请求或外部 API 的输入时，必须通过 `zod` schema 验证
- 遇到需要权限判断的操作，先查 `src/middleware/auth.ts` 确认有鉴权中间件
- 涉及删除、状态变更等不可逆操作时，确认有操作日志记录

### 测试
- 新增 service 方法时，同步创建对应的 `*.test.ts` 文件
- 看到测试文件缺少边界条件测试（null、空数组、超出范围的值）时，主动补充
- Mock 所有外部依赖（数据库、HTTP 请求），不让测试依赖真实环境

---

## 禁止行为
- 不要使用 `any` 类型（如必须使用，添加注释说明原因）
- 不要在 routes 层写业务逻辑，业务逻辑放在 services 层
- 不要直接在代码中硬编码配置值，使用 `src/config` 模块
- 不要使用 `console.log`，使用项目的 `logger` 工具
- 不要使用 `@ts-ignore`（除非有充分理由并注释）
- 不要拼接原始 SQL，必须通过 ORM

---

## 测试规范
- 每个 service 方法都要有对应单元测试
- 测试文件放在与被测文件同目录，命名为 `*.test.ts`
- Mock 外部依赖（数据库、HTTP 请求）

---

## 当前开发重点
[可选：填写当前迭代的重点功能或需要特别注意的模块]
