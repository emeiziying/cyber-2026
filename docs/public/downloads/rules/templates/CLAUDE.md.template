# [项目名称] — AI 助手规则

## 项目背景
[简短描述：这是什么项目，主要功能，目标用户]

---

## 技术栈
- **语言：** TypeScript 5.x（严格模式）
- **运行时：** Node.js 20+
- **框架：** [Express / Fastify / NestJS]
- **数据库：** [PostgreSQL via Prisma / MongoDB / ...]
- **测试：** [Vitest / Jest]
- **CI/CD：** [GitHub Actions / ...]

---

## 常用命令
```bash
# 开发
npm run dev

# 测试
npm test
npm run test:coverage

# 构建
npm run build

# 数据库迁移
npx prisma migrate dev
```

---

## 目录结构
```
src/
├── routes/         # 路由定义（仅路由，不含业务逻辑）
├── services/       # 业务逻辑层
├── repositories/   # 数据访问层
├── middleware/     # Express 中间件
├── utils/          # 工具函数
└── types/          # TypeScript 类型定义
```

---

## 代码规范

### 命名约定
- 文件名：`kebab-case`（如 `user-service.ts`）
- 类名：`PascalCase`
- 函数/变量：`camelCase`
- 常量：`UPPER_SNAKE_CASE`
- 数据库字段：`snake_case`

### 接口返回格式
统一使用以下格式：
```typescript
// 成功
{ data: T, error: null }
// 失败
{ data: null, error: { code: string, message: string } }
```

### Import 顺序
1. Node.js 内置模块
2. 第三方库
3. 内部模块（绝对路径 `@/`）
4. 相对路径模块

---

## 行为规范（用触发器格式描述，比形容词更精确）

> 格式：在 [具体场景] 时，[具体行为]

### 代码质量
- 函数体超过 50 行时，拆分为职责单一的子函数
- 看到超过 3 层嵌套的回调或 if 时，重构为 async/await 或提前 return
- 发现重复逻辑出现 3 次以上时，提取为工具函数并放入 `src/utils/`
- 写新功能前，先用 Grep 搜索是否有可复用的现有实现

### 错误处理
- 遇到 try-catch 只写 `console.log` 的情况，要求补充错误上报（用 `logger.error`）
- API 接口发生错误时，返回通用错误信息给前端，详细错误写入日志，绝不暴露堆栈或数据库报错
- 调用外部服务时，确保有超时设置和失败重试逻辑

### 安全
- 处理任何来自用户、HTTP 请求或外部 API 的输入时，必须通过 `zod` schema 验证
- 遇到需要权限判断的操作，先查 `src/middleware/auth.ts` 确认有鉴权中间件
- 涉及删除、状态变更等不可逆操作时，确认有操作日志记录

### 测试
- 新增 service 方法时，同步创建对应的 `*.test.ts` 文件
- 看到测试文件缺少边界条件测试（null、空数组、超出范围的值）时，主动补充
- Mock 所有外部依赖（数据库、HTTP 请求），不让测试依赖真实环境

---

## AI 开发禁区

以下模块和操作**不允许 AI 直接编写或修改**，AI 只能做分析、建议方案和生成测试：

### 绝对禁区（AI 不可触碰实现代码）
- **认证/鉴权核心逻辑：** Token 签发与校验、密码加密与比对、OAuth 回调处理、权限中间件核心判断
- **支付/财务计算：** 金额计算、退款逻辑、对账流程——精度和业务规则必须人工确认
- **数据库迁移脚本：** 改表结构、删字段、数据迁移——不可逆，线上执行出错无法回滚
- **加密/密钥管理：** 密钥生成、证书配置、加密算法选型——AI 容易选用过时或不安全的方案
- **生产环境配置：** 数据库连接串、第三方密钥、`.env` 文件——泄露即事故
- **合规与隐私处理：** 用户数据脱敏规则、GDPR 删除逻辑、日志中敏感字段过滤
- **CI/CD 流水线配置：** 部署脚本、发布流程——改错一行可能导致代码直接上生产
- **外部系统对接：** 银行接口、政府数据接口、支付网关——签名算法和报文格式有严格规范

### AI 可参与但必须人工确认
- 安全模块改动：AI 可分析代码和建议方案，但实现和合并必须人工完成
- 数据库 schema 变更：AI 可起草迁移脚本，但必须人工审核影响范围和回滚方案
- 接口权限配置：AI 可列出权限矩阵草稿，但每个角色的权限边界必须人工确认
- 依赖版本升级（大版本）：AI 可分析兼容性，但升级决策和验证必须人工主导

---

## 安全约束

### 输入校验
- 所有来自用户、HTTP 请求、外部 API 的输入必须通过 `zod` schema 验证
- 不信任任何客户端数据，包括 header、cookie、query 参数

### 数据库安全
- 不要拼接原始 SQL，必须通过 ORM 或参数化查询
- 批量操作必须有数量上限，防止意外全表更新/删除
- 数据删除优先使用软删除（标记删除），硬删除需要审批

### 敏感数据
- 日志不输出用户密码、手机号、身份证号等敏感字段
- API 返回不泄露内部错误堆栈和数据库报错，使用通用错误信息
- 不在代码中硬编码任何密钥、Token 或连接串，使用环境变量

### 接口安全
- 关键接口（登录、短信验证码、密码重置）必须有速率限制
- 文件上传必须校验类型和大小，路径拼接防止目录穿越
- 调用外部服务时必须设置超时和失败处理

---

## 禁止行为
- 不要使用 `any` 类型（如必须使用，添加注释说明原因）
- 不要在 routes 层写业务逻辑，业务逻辑放在 services 层
- 不要直接在代码中硬编码配置值，使用 `src/config` 模块
- 不要使用 `console.log`，使用项目的 `logger` 工具
- 不要使用 `@ts-ignore`（除非有充分理由并注释）
- 不要拼接原始 SQL，必须通过 ORM

---

## 测试规范
- 每个 service 方法都要有对应单元测试
- 测试文件放在与被测文件同目录，命名为 `*.test.ts`
- Mock 外部依赖（数据库、HTTP 请求）

---

## 当前开发重点
[可选：填写当前迭代的重点功能或需要特别注意的模块]
